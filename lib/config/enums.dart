enum AuthType {
  login,
  logOut,

  signup,
  socialMedia,
  completeProfile,
  forgotPassword,
  otpVerify,
  resetPassword,
  addPlatform,
}

enum LocationType { loading, updated, error }

enum Setting {
  contactUs,
  deleteAccount,
}

enum otherProfile { otherUserProfile }
